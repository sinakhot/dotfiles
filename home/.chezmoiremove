# Targets chezmoi should delete on apply (removed from the managed set).
# Paths are relative to the home dir (~).

# zellij → replaced by herdr (ubi:ogulcancelik/herdr)
.config/zellij

# yazi → removed
.config/yazi
